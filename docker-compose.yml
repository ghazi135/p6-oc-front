# Production: build and serve Angular app with Nginx.
# Usage: docker compose up -d
# App: http://localhost (or http://localhost:${APP_PORT} if APP_PORT is set)
name: olympic-games-starter

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-80}:80"
